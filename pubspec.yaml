name: chuk_ui
description: A standalone, token-driven UI component library for Flutter. Build your own design system without Material.
version: 0.1.0
repository: https://github.com/chukfinley/chuk_ui
issue_tracker: https://github.com/chukfinley/chuk_ui/issues
topics:
  - ui
  - design-system
  - widgets
  - components

environment:
  sdk: ^3.9.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
